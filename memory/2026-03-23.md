## Notes
- Bootstrapped in a blank workspace; identity and USER details are still unknown until the human provides them.
- Human says there are prior assistant files from an earlier install and is trying to locate / restore them (possibly from a GitHub or local backup); I’m waiting on the path or repo info to pull them in.
- Recovered the old workspace from `C:\Users\steve\workspace`, restored GitHub remote/history, and confirmed the latest surviving repo snapshot was from Mar 19; post-Mar-19 work had been lost in an uninstall/reset.
- Rebuilt the recovery layer: `.gitignore` now protects local secrets, added backup/restore docs + scripts, created and exported fresh workspace backups to Windows Downloads, set a daily backup cron, and pushed recovery commits to GitHub.
- Captured/restored operational account info for Gmail, Google OAuth, Gumroad, Substack, and Coinbase into local secrets/docs; also archived the Atlas Pulse Gumroad pack assets and documented the Substack/Gumroad publication/product details.
- Reframed the machine with new specs: `OPERATING_SPEC.md`, `SCANNER_V2_SPEC.md`, `COINBASE_WS_SPEC.md`, full dashboard specs/layout/data/style/build docs, stream content spec, center panel spec, paper-trader center spec, paper trader v2 spec, profit ladder spec, and system control spec.
- Fixed hardcoded `/data/.openclaw/workspace` runtime paths across the pipeline to `/home/lokiai/.openclaw/workspace`; lint and pre-cycle validation passed after the pathing repair.
- Brought data generation back online: scanner-only backlog cron running every 15 minutes, Coinbase websocket service built and running as a separate ingest process, logging cache/state + rolling snapshots.
- Rebuilt the dashboards as separate apps: `dashboard/operator_main.py` on port 8500 and `dashboard/stream_main.py` on port 8501. Operator dashboard now reads scanner/websocket/Paper Trader V2 state; stream dashboard has a locked layout, real BTC-USD 5m candles, tactical mission display, and improved public-facing copy.
- Built Paper Trader V2 as a new trader aligned to the current machine: 3-slot model, scanner + websocket intake, Tier A/B gating, stop/timeout/no-move exits, profit ladder/trailing/fake-pump framework, move-character labeling, audit summary, and new V2 state files. It opened and later closed an ETH paper slot on `no_move`, proving the lifecycle logic works.
- Wired Paper Trader V2 into both dashboards and cleaned the operator command bay/state naming to canonical subsystem labels. Had to kill stale old dashboard processes multiple times because port 8500 was still being served by the legacy app, causing misleading UI mismatches until corrected.
- Rebuilt and published a first LokiAI-voice Substack post: `I Rebuilt My Market Machine Today` → https://lokiai.substack.com/p/i-rebuilt-my-market-machine-today
- Built Social / Intel Pulse V1, fixed the broken scanner cron wrapper, and wired a clean modern ordered loop: scanner → social/intel pulse → Paper Trader V2 → ops alerts.
