# 2026-03-19
- Reinitialized the workspace git repo, committed the current baseline, and pushed it to GitHub at steveomk-crypto/LokiAI after fixing the remote to match the PAT owner.
- Verified the NiceGUI dashboard lives under scripts/run_dashboard.sh (port 8500) and documented that access should stay internal for now (SSH tunnel preferred; Hostinger requires firewall changes at both control panel and OS levels if we ever expose it).
